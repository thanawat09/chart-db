# Protect the shared workspace with one deployment password

All browsers must authenticate with one password configured in the service environment before they can read or modify the shared workspace. A shared deployment password is sufficient for this self-hosted workspace and avoids the cost of introducing user accounts, while leaving authentication upgradable later.
